#include <bits/stdc++.h>

using namespace std;

int main(){
    int n, m, tmp, pont;
    vector<int> a, b;
    cin >> n;
    for(int i = 0; i < n; i++){
        cin >> tmp;
        a.push_back(tmp);
    }
    cin >> m;
    for(int i = 0; i < m; i++){
        cin >> tmp;
        b.push_back(tmp);
    }
    
    for(int i = a;){

    }

    a.

    return 0;
}   